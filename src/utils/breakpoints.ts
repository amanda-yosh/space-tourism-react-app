const breakpointsKey = {
    desktop: 1440,
    tablet: 768,
    mobile: 375,
}

const measureUnit = 'px'

export const breakpoints = {
    desktop: breakpointsKey.desktop + measureUnit,
    tablet: breakpointsKey.tablet + measureUnit,
    mobile: breakpointsKey.mobile + measureUnit,
}
